[project]
name = "walmart-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shubham Arya", email = "shubhamstudy11@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "airflow-operators>=0.11.0",
    "apache-airflow>=3.3.2",
    "databricks-sdk>=0.47.0",
    "dbt-core>=1.12.4",
    "dbt-databricks>=1.10.9",
]

[project.scripts]
walmart-dbt = "walmart_dbt:main"

[build-system]
requires = ["uv_build>=0.12.12,<0.13.0"]
build-backend = "uv_build"
