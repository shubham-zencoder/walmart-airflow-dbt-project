walmart_project:
  outputs:
    dev:
      catalog: walmart
      host: "{{ env_var('DATABRICKS_HOST') }}"
      token: "{{ env_var('DATABRICKS_TOKEN') }}"
      http_path: /sql/1.0/warehouses/8408f3ae286943fb
      schema: dbt_schema
      threads: 1
      type: databricks
  target: dev
