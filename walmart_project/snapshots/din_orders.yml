snapshots:
  - name: dim_orders
    relation: ref('eph_orders') # source('my_source', 'my_table') or ref('my_model')
    
    config:
      database: walmart
      schema: gold
      strategy: timestamp
      unique_key: order_id
      updated_at: order_updated_timestamp
      dbt_valid_to_current: "to_date('9999-12-31')"